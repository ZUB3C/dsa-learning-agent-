# 🎓 АиСД Learning Platform: Интеллектуальная платформа для изучения алгоритмов и структур данных

Учебный проект, демонстрирующий применение **агентных систем на базе LLM** для персонализированного обучения алгоритмам
и структурам данных. Платформа адаптирует материалы под уровень студента, генерирует тесты, проверяет решения и
оказывает психологическую поддержку.

***

## 🧭 Основная идея

Проект реализует полноценную образовательную экосистему с использованием **FastAPI**, **LangChain**, **RAG** 
(Retrieval-Augmented Generation) и **двух LLM-провайдеров** (GigaChat для русского языка, DeepSeek для английского).

**Ключевые возможности:**

- Первичная оценка уровня знаний студента
- Адаптивная подача учебных материалов через RAG
- Генерация персонализированных тестов и задач
- Двойная верификация ответов пользователя на тесты (снижение галлюцинаций)
- Автоматический выбор LLM в зависимости от языка и типа запроса
- Психологическая поддержка для управления стрессом

***

## 📁 Структура проекта

```
src/
├── agents/                         # Агентная система (LangChain)
│   ├── __init__.py                 
│   ├── llm_router_agent.py         # Роутер для выбора подходящей LLM по языку и типу запроса
│   ├── materials_agent.py          # Агент для адаптации учебных материалов под уровень студента
│   ├── registry.py                 # Реестр всех доступных агентов для удобной загрузки
│   ├── support_agent.py            # Агент психологической поддержки студентов
│   ├── test_generation_agent.py    # Агент генерации тестов и задач по темам
│   └── verification_agent.py       # Агент проверки ответов с двойной верификацией
├── core/                           # Ядро системы
│   ├── __init__.py                 
│   ├── database.py                 # Модели SQLAlchemy (User, Test, Assessment и др.)
│   ├── llm.py                      # Инициализация LLM-провайдеров (GigaChat, DeepSeek)
│   └── vector_store.py             # Менеджер ChromaDB для RAG-поиска материалов
├── data_processing/                # Обработка данных для RAG
│   ├── __init__.py                 
│   ├── db_populator.py             # Заполнение ChromaDB документами из PDF с метаданными
│   ├── pdf_parser.py               # Парсинг LaTeX-PDF с извлечением оглавления и структуры
│   └── text_splitter.py            # Умное разбиение текста на чанки с извлечением концепций
├── models/                         # Pydantic-модели
│   ├── __init__.py                 
│   └── schemas.py                  # Схемы запросов и ответов для всех API эндпоинтов
├── routers/                        # REST API маршруты
│   ├── __init__.py               
│   ├── assessment.py               # API первичной оценки знаний студента
│   ├── health.py                   # Healthcheck эндпоинт для мониторинга
│   ├── llm_router.py               # API выбора и использования LLM для различных задач
│   ├── materials.py                # API получения и генерации учебных материалов
│   ├── support.py                  # API психологической поддержки студентов
│   ├── tests.py                    # API генерации и отправки тестов
│   └── verification.py             # API проверки ответов с двойной верификацией
├── scripts/                        # Утилиты и скрипты
│   └── populate_db.py              # CLI-скрипт для заполнения ChromaDB из PDF
├── config.py                       # Конфигурация через Pydantic Settings (.env)
└── main.py                         # Точка входа FastAPI приложения
```

***

## ⚙️ Установка и запуск

### 1. Установить uv

**uv** — современный пакетный менеджер для Python, написанный на Rust (в 10x быстрее pip).

**Установка (Linux/macOS):**

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

**Установка (Windows):**

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**Или через pipx:**

```bash
pipx install uv
```

Официальная
документация: [https://docs.astral.sh/uv/getting-started/installation/](https://docs.astral.sh/uv/getting-started/installation/)

### 2. Клонировать проект

```bash
git clone https://github.com/ZUB3C/dsa-learning-agent.git
cd dsa-learning-agent
```

### 3. Установить зависимости

```bash
uv sync
```

Эта команда автоматически создаст виртуальное окружение и установит все зависимости из `pyproject.toml`.

### 4. Настроить `.env`

Скопировать шаблон конфигурации:

```bash
cp .env.example .env
```

Заполнить API-ключи в `.env`:

```env
# GigaChat (Russian)
GIGACHAT_API_KEY=your_gigachat_api_key_here
GIGACHAT_MODEL=GigaChat
GIGACHAT_BASE_URL=https://gigachat.devices.sberbank.ru/api/v1

# DeepSeek (English)
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# LLM Settings
LLM_TEMPERATURE=0.2
TIMEOUT_S=60

# ChromaDB
CHROMA_PERSIST_DIRECTORY=./chroma_db
CHROMA_COLLECTION_NAME=aisd_materials

# RAG
RAG_TOP_K=3

# Database population settings
PDF_CHUNK_SIZE=1000
PDF_CHUNK_OVERLAP=200
```

### 5. Заполнить базу знаний (ChromaDB)

Вначале надо сохранить файл [algobook.pdf](https://www.babichev.org/books/AlgoBook.pdf) в корень проекта.

**Запуск скрипта для заполнения векторной БД из PDF:**

```bash
uv run python -m src.scripts.populate_db --pdf algobook.pdf --clear
```

**Параметры:**

- `--pdf` — путь к PDF файлу (по умолчанию: `algobook.pdf`)
- `--clear` — очистить существующую коллекцию перед заполнением
- `--chunk-size` — размер чанка в символах (по умолчанию: 1000)
- `--chunk-overlap` — перекрытие чанков (по умолчанию: 200)

### 6. Запустить приложение (API)

```bash
uv run python -m src.main
```

Откройте документацию Swagger UI:
👉 [http://127.0.0.1:8001/docs](http://127.0.0.1:8001/docs)

***

### 6. Запустить создание отчёта с примерами работы API 

```bash
uv run python -m src.scripts.generate_endpoint_report
```

Полученный отчёт сохраняется в файл `api-examples-report.md`.

***

***

## 🧩 API Endpoints

| Модуль           | Метод  | Эндпоинт                                 | Описание                               |
|------------------|--------|------------------------------------------|----------------------------------------|
| **System**       | `GET`  | `/health/`                               | Проверка состояния API                 |
| **Assessment**   | `POST` | `/api/v1/assessment/start`               | Начать первичное тестирование          |
| **Assessment**   | `POST` | `/api/v1/assessment/submit`              | Отправить результаты тестирования      |
| **Assessment**   | `GET`  | `/api/v1/assessment/results/{user_id}`   | Получить результаты оценки             |
| **Materials**    | `POST` | `/api/v1/materials/get-materials`        | Получить адаптированные материалы      |
| **Materials**    | `POST` | `/api/v1/materials/ask-question`         | Задать вопрос по материалу             |
| **Materials**    | `POST` | `/api/v1/materials/generate-material`    | Сгенерировать учебный материал         |
| **Materials**    | `POST` | `/api/v1/materials/add-custom-topic`     | Добавить пользовательскую тему         |
| **Materials**    | `GET`  | `/api/v1/materials/topics`               | Список доступных тем                   |
| **Materials**    | `POST` | `/api/v1/materials/search`               | Поиск материалов                       |
| **Tests**        | `POST` | `/api/v1/tests/generate`                 | Сгенерировать тест по теме             |
| **Tests**        | `POST` | `/api/v1/tests/generate-task`            | Сгенерировать задачу                   |
| **Tests**        | `POST` | `/api/v1/tests/submit-for-verification`  | Отправить тест на проверку             |
| **Tests**        | `GET`  | `/api/v1/tests/{test_id}`                | Получить тест по ID                    |
| **Tests**        | `GET`  | `/api/v1/tests/user/{user_id}/completed` | Завершенные тесты пользователя         |
| **Verification** | `POST` | `/api/v1/verification/check-test`        | Проверка ответа с двойной верификацией |
| **Verification** | `GET`  | `/api/v1/verification/history/{user_id}` | История проверок пользователя          |
| **LLM Router**   | `POST` | `/api/v1/llm-router/select-and-generate` | Выбрать модель и сгенерировать контент |
| **LLM Router**   | `GET`  | `/api/v1/llm-router/available-models`    | Список доступных моделей               |
| **LLM Router**   | `POST` | `/api/v1/llm-router/route-request`       | Маршрутизация запроса к модели         |
| **Support**      | `POST` | `/api/v1/support/get-support`            | Получить психологическую поддержку     |
| **Support**      | `GET`  | `/api/v1/support/resources`              | Ресурсы поддержки                      |
| **Support**      | `POST` | `/api/v1/support/feedback`               | Отправить обратную связь               |

***

## 🧠 Примеры использования

### Примеры использования всех API Endpoints находятся в файле [api-examples-report.md](api-examples-report.md)

***

## 🧰 Агентная система

| Агент                            | Файл                       | Назначение                                            |
|----------------------------------|----------------------------|-------------------------------------------------------|
| **LLM Router**                   | `llm_router_agent.py`      | Выбор подходящей LLM по языку и типу запроса          |
| **Materials Agent**              | `materials_agent.py`       | Адаптация материалов под уровень студента             |
| **Question Answering Agent**     | `materials_agent.py`       | Ответы на вопросы с учетом контекста из RAG           |
| **Test Generation Agent**        | `test_generation_agent.py` | Генерация тестов и задач по темам                     |
| **Verification Agent**           | `verification_agent.py`    | Первичная проверка ответов                            |
| **Secondary Verification Agent** | `verification_agent.py`    | Вторичная проверка другой моделью (анти-галлюцинации) |
| **Support Agent**                | `support_agent.py`         | Психологическая поддержка и рекомендации              |

---

## 🔬 Технологический стек

- **Backend:** FastAPI + Uvicorn
- **LLM Orchestration:** LangChain
- **LLM Providers:** GigaChat (русский), DeepSeek (английский)
- **Vector Database:** ChromaDB + HuggingFace Embeddings
- **Relational Database:** SQLite (SQLAlchemy ORM)
- **PDF Processing:** PyMuPDF (для парсинга LaTeX-PDF)
- **Package Manager:** uv
- **Task Runner:** just

***

## 📊 RAG Pipeline

**Процесс заполнения базы знаний:**

1. **Парсинг PDF** (`pdf_parser.py`) — извлечение оглавления и структуры разделов
2. **Разбиение на чанки** (`text_splitter.py`) — умная сегментация с извлечением концепций
3. **Векторизация** (`vector_store.py`) — создание embeddings через HuggingFace
4. **Хранение** — сохранение в ChromaDB с метаданными (тема, уровень, иерархия)

**Процесс поиска материалов:**

1. Пользовательский запрос → векторизация
2. Similarity search в ChromaDB (top-k документов)
3. Передача контекста в Materials Agent
4. Адаптация материалов под уровень студента
5. Генерация ответа через LLM

***

## 🧩 Middleware и метрики

Каждый запрос оборачивается в middleware, добавляющий в заголовки время выполнения:

```
X-Process-Time: 0.154
```

***

## 🔧 Форматирование кода

Проект использует **just** — современный command runner для автоматизации задач.

**Установка just:**

```bash
# macOS
brew install just

# Linux
cargo install just

# Windows
scoop install just
```

Документация: [https://just.systems/man/en/](https://just.systems/man/en/)

**Запуск форматирования:**

```bash
just format
```

## 💊 Создание отчёта для метрик агента верификации

```bash
uv run python -m src.scripts.measure_secondary_verification --test-data test_data.json
```
